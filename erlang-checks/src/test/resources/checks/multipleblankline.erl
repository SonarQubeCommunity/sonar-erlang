-module(a).



-export([hello/0]).


hello()->
	Code,


	Code,

 	%comment
	%comment
	%comment

	DifferentBlock,

	
	%comment
	%comment
	%comment	
	
	Whatever1,

	%comment
	%comment
	%comment


	Whatever2.

hello([])->
	[
		error,
		berror
	].


-ifdef(TEST).
    -export([
                get_config_file/0,
                get_config_file/1
        ]).
-endif.

%%--------------------------------------------------------------------
%% @doc
%% Starts an application and its deps.
%% Returns a function to stop every application that was started by this call
-spec start(atom()) -> {ok, fun(() -> ok)}.
bello() ->
	%so
	%this comment
	%is still ok
	Code.
